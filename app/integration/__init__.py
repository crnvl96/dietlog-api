"""Integration module for external services.

This package contains implementations of services that integrate with external APIs and systems,
including LLM providers (like Anthropic) and image services (like HTTPX). These implementations
conform to the service interfaces defined in the service package.
"""
